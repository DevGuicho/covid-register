<!DOCTYPE html>
<html lang="es">
  <head>
    <%- include('../partials/head'); %>
  </head>
  <body class="d-flex flex-column min-vh-100 justify-content-between">
    <header><%- include('../partials/header', {page:'list'}); %></header>
    <div class="d-flex flex-column align-items-center container py-4">
      <h1 class="mb-4"><%= data.name %> <%= data.lastname %></h1>
      <div class="card" style="width: 30rem">
        <div class="card-body">
          <h4 style="font-weight: bold" class="card-title">
            Id: <%= data.id %>
          </h4>
        </div>
        <ul class="list-group list-group-flush">
          <li class="list-group-item">
            <h6 style="font-weight: bold">CURP</h6>
            <%= data.curp %>
          </li>
          <li class="list-group-item">
            <h6 style="font-weight: bold">Edad</h6>
            <%= data.age %>
          </li>
          <li class="list-group-item">
            <h6 style="font-weight: bold">Sexo</h6>
            <%= data.gender %>
          </li>
          <li class="list-group-item">
            <h6 style="font-weight: bold">Fecha de ingreso</h6>
            <%= data.admissionDate %>
          </li>
          <li class="list-group-item">
            <h6 style="font-weight: bold">Fecha primeros síntomas</h6>
            <%= data.symptomDate %>
          </li>
          <li class="list-group-item">
            <h6 style="font-weight: bold">Teléfono de emergencia</h6>
            <%= data.contactNumber %>
          </li>
          <li class="list-group-item">
            <h6 style="font-weight: bold">Camilla</h6>
            <%= data.gurney %>
          </li>
          <li class="list-group-item">
            <h6 style="font-weight: bold">Intubado</h6>
            <% if(data.isIntubated){ %> Sí <% } else{ %> No <% } %>
          </li>
        </ul>
        <div class="card-body">
          <form
            class="d-inline"
            action="/update-patient/<%= data.id %>"
            method="GET"
          >
            <button
              value="submit"
              type="submit"
              class="btn btn-outline-primary"
            >
              Editar
            </button>
          </form>
          <form
            class="d-inline"
            action="/api/delete-patient/<%= data.id %>"
            method="GET"
          >
            <button value="submit" type="submit" class="btn btn-danger">
              Eliminar
            </button>
          </form>
        </div>
      </div>
    </div>
    <footer><%- include('../partials/footer'); %></footer>
  </body>
</html>
